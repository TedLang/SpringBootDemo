my:
  name: ted
  age:  29
  number: ${random.int}
  uuid: ${random.uuid}
  max: ${random.int(10)}
  value: ${random.value}
  greeting: hi, i'm ${my.name}


spring:
  profiles:
    active: dev

  datasource:
    url: jdbc:mysql://localhost:3306/test
    username: root
    password: 123456
    dirver-class-name: com.mysql.jdbc.Driver